[project]
name = "formula-se-backend"
version = "0.1.0"
description = "Formula SE — Space Engineers campaign management backend"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"
